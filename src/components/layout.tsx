import { Typography } from "@mui/material";

const Layout = () => {
  return (
    <div className="pb-5">
      <Typography variant="h4" className="font-bold">
        NBA STAT
      </Typography>
    </div>
  );
};

export default Layout;
